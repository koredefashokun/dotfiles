# This file configures moving over dotfiles into their appropriate place on the host system.
# (At least, that's what I'm creating it to do)
# N.B. This file is untested, because I do not intend to automagically override my present vim config
# (Run at your own risk)

# Move the vimrc into the user's root
mv .vimrc ~

